# 2026-09-01

## P1 source baseline 재검증과 checkpoint 준비

- 기존 작업 트리 157개와 이 checkpoint 작업일지까지 158개 경로를 P1-3/P1-4/P1-5, exact guild-only
  registry와 후속 canonical 보정 범위에 대조했다.
  승인 범위 밖 경로, runtime artifact, credential, key/cert, DB, log, media와 private user data는 0개였고
  submodule pin은 변경되지 않았다.
- checkout에 이미 구현된 test archive key/TLS/ACL provisioner, dirty checkout의 exact current-source
  snapshot/image provenance와 fresh physical mic-OFF 뒤 consent inactive reconcile 경로를 코드·테스트와 대조했다.
- 최소 직접 소유 검증은 `181 passed, 72 subtests`, live prerequisite 집중 검증은
  `205 passed, 44 subtests`였다. Python 3.11 canonical은
  **`5153 passed, 18 skipped, 1584 subtests, 1 warning, 0 failed`**로 통과했다. 경고는 Python 3.13에서
  제거 예정인 기존 `audioop` 1건이다.
- `git diff --check`는 통과했다. 이번 검증에서 Discord, Docker, microphone, GPU, Minecraft와 production
  archive는 기동하거나 변경하지 않았다. source/offline 통과는 실제 host provision, image build/load,
  Gateway/ephemeral/archive 저장 E2E의 live 증거가 아니다.
- 다음 순서는 별도 live 승인 뒤 test archive actual provision → current-source/fresh mic-OFF preflight →
  Discord same-user/stale-session/delivery/ephemeral → 실제 Qwen 24-row와 grounded canary 10건이다.
