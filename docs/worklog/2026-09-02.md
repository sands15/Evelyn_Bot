# 2026-09-02

## Discord feedback live host preflight와 recovery 보존

- 실제 host에서 두 고정 대상 volume이 fixed NTFS·healthy·서로 다른 disk인 것은 확인했지만, C:/D: 모두
  File Explorer의 현재 메뉴가 `BitLocker 켜기`였다. test provisioner 두 시도는 비영 종료했고 root/owner
  marker/key·TLS를 하나도 만들지 않았다. 이 결과는 `On/FullyEncrypted/Unlocked` gate의 정상 fail-close다.
- live launcher의 cleanup 실패가 exact command recovery ledger까지 지우던 경로를 수정했다. 이제 선행 cleanup
  failure가 하나라도 있으면 run root를 보존하고 완전한 정상 cleanup에서만 제거한다. checkpoint commit은
  `6df290c`; launcher 회귀는 `10 passed`, dry-run은 content-free `live_confirmation_required`였다.
- 기존 DPAPI credential은 메모리에서만 사용해 현재 Discord target/principal을 read-only로 유일 해석했다.
  ID·이름·token은 출력하거나 문서화하지 않았고 command registry도 변경하지 않았다.
- 현재 launcher의 운영자 `PASS/FAIL` 한 줄은 Gateway 기동과 cleanup receipt일 뿐, same-user·wrong-user·
  stale-session·delivery failure·ephemeral 180초 결과를 시나리오별로 기계 결박하지 않는다. 따라서 한 번의
  `PASS`만으로 P1-4/P1-5 전체 live gate를 닫지 않는다.
- Docker/WSL, Evelyn service, fresh microphone supervisor와 Discord Gateway는 시작하지 않았고 production
  archive는 OFF를 유지했다. 이 시점에는 volume 암호화·recovery를 다음 단계로 판단했지만 아래에서 교정했다.

## 선택 기능을 필수 작업으로 오판한 기록과 교정

- 기존 장기기억은 이미 Obsidian-compatible Markdown vault를 durable source로 사용한다. private archive는
  30일 exact text/final STT·참여 event·사용자별 열람/삭제를 위한 별도 선택 기능인데, 미완료 plan 항목만 보고
  현재 필수 작업으로 잘못 판단해 BitLocker preflight로 진행했다.
- 영향은 read-only host 확인과 fail-close provision 시도, launcher recovery 보존 수정·문서화까지다. BitLocker,
  archive root/key/TLS, Docker/Evelyn service와 Discord Gateway의 실제 변경·기동은 0이다.
- 교정: private archive와 BitLocker는 기본 OFF로 유지한다. 사용자가 exact 30일 원문 기록과 사용자별
  열람·삭제를 명시적으로 요구하지 않으면 재개하지 않는다. 기억 관련 후속은 기존 Markdown vault의
  deletion-current lineage와 live recall을 별도 범위로 다룬다.
- 재발 방지: `진행해`를 미완료 계획 전체의 승인으로 해석하지 않는다. 먼저 현재 사용자 목표가 기존 기능으로
  충족되는지 확인하고, 선택 기능·보안 설정·외부 효과는 목적과 필요성을 명시적으로 확인한다.

## 기존 Markdown 명시적 기억의 재시작 검증

- 실제 사용자 기억을 열거나 바꾸지 않고 임시 저장소와 비민감 synthetic 문구만 사용했다. 저장 뒤 새 Python
  프로세스에서 canonical local owner 귀속 회상하고, 2단계 삭제 뒤 다시 새 프로세스에서 비회상을 확인했다.
- 같은 회상 요청에서 `owner_scope`를 생략하면 저장된 노트가 있어도 차단됐다. 이는 principal 격리의
  fail-closed 동작이며, 정확한 scope를 전달한 실제 경로는 정상 회상됐다.
- 기존 lifecycle·guild reset·삭제 crash/restart 집중 테스트 3개도 `3 passed in 2.41s`였다. 임시 저장소는
  종료 시 제거됐고 실제 `bot_memory`의 내용은 읽지 않았으며 전후 metadata-only fingerprint가 같았다.
- 이 결과는 명시적 user-confirmed Markdown 경로의 source/offline 검증이다. 자동 파생 기억의
  deletion-current lineage 복구나 실제 Fast/Discord 대화의 live 검증을 의미하지 않는다.

## 자동 파생 Markdown 회상 경계 검증

- 실제 기억을 읽거나 수정하지 않고 임시 저장소와 synthetic 문구만 사용했다. automatic daily에서
  deterministic episode와 semantic note를 실제 생성한 뒤 같은 guild 조회를 검사했다.
- path, reset scope, source ref, current parent와 body hash를 묶은 제한적 허용안을 검토했지만,
  writer가 room/person/session label을 guild/date Markdown 하나에 합치고 owner binding을 보존하지
  않는 경계를 재현했다. 삭제 계보만 강화하면 person-bound 원문이 guild 공용 기억으로 노출될 수 있다.
- 안전하지 않은 허용 변경은 남기지 않았다. lineage-shaped 입력으로 생성한 두 automatic child가 같은
  guild에서도 회상되지 않는 회귀를 추가해 기존 fail-closed 정책을 고정했다.
- memory vault `63 passed, 4 subtests`, lifecycle·삭제 재시작 `44 passed, 17 subtests`, prompt·context
  `30 passed, 3 subtests`, write policy·archive purge `36 passed`로 합계 `173 passed, 24 subtests`, 실패 0이다.
- 재활성화 조건은 owner별 durable source 분리, exact turn lineage/purge, cache·vector·graph와 새 프로세스의
  삭제 후 비회상이다. 실제 `bot_memory`, 화면, Discord·마이크·Docker 등 외부 서비스는 건드리지 않았다.
