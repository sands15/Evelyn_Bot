# 2026-08-12

## `/mic on` voice-validation drawer visibility

- 실행 중 Control Page에서 `/mic on` 응답과 `voice_validation/open` panel command가
  Bot API와 public proxy까지 동일하게 전달됐지만, 브라우저 consumer가 숨은 drawer
  내부 버튼에 scroll·focus만 적용해 오른쪽 패널은 계속 화면 밖에 남는 경계를 확인했다.
- command consumer가 drawer에 idempotent `open` class를 먼저 추가한 뒤 기존 검증 시작
  버튼 scroll·focus를 수행하도록 수정했다. 마이크 ON이나 consent preview/apply는 직접
  실행하지 않아 명시적 청취 동의 경계는 그대로다.
- Node 행동 회귀는 `open -> scroll -> focus` 순서와 같은 command 재적용의 무중복을
  고정한다. UI 집중 40개와 CI-equivalent 전체 3,253개(skip 22)가 통과했다. 실행 중
  Control Page를 새로고침한 브라우저에서 drawer open, opacity 1, 검증 시작 버튼의
  화면 표시·focus를 확인했다. 마이크·스피커·Discord·Minecraft·Docker는 시작하지 않았다.

## Runtime error observability false-clear

- 실행 중 Runtime Health는 optional service 때문에 degraded였지만 오류 패널은 clear/0이었다.
  선택 서비스 OFF 자체는 예외가 아니므로 그대로 두고, 실제 Fast Control continuity 오류
  artifact가 collector와 public source allowlist에서 빠진 경계를 별도로 확인했다.
- `fastControlContinuity`를 정식 source로 추가했다. 필수 runtime service
  probe가 counter payload 없이 실패하면 `currentErrorCount`에는 반영하되 기록된 예외
  `totalCount`는 올리지 않는다. 이 합성은 Control Page·Bot API·세 LLM·TTS·STT에
  적용하며 UI도 두 숫자를 명시적으로 구분한다.
- Optional payload-less failure는 durable desired-state가 없어 의도 OFF와 예상 밖 장애를
  구분하지 못하므로 Runtime Health에만 남긴다. 현재 Python backend는 재시작하지 않아
  live 오류 API에는 아직 이전 source 목록이 보이고, 정적 UI의 새 합계 문구만 반영됐다.
- 현재 artifact에 새 collector를 read-only로 적용해 Fast Control source가 available/ready로
  잡히고 전체 source가 9개로 늘어나는 것을 확인했다. focused 40개, Runtime Health·Control
  Page 인접 159개(skip 2), CI-equivalent 전체 3,255개(skip 22), Python/JavaScript 구문과
  diff check가 통과했다. 실행 중 서비스는 재시작하지 않았다.

## Autonomy restart route ordering

- 권한 만료 뒤 state는 `authorization_required`지만 이전 loop task cleanup이 아직 남은 창에서
  보존된 Minecraft route intent를 가진 Discord `자율시작`이 route를 먼저 연결하면, 뒤늦은
  old cleanup이 새 child를 끊고도 시작 성공 문구를 반환할 수 있던 순서를 합성 회귀로 재현했다.
- handler는 state label과 관계없이 engine stop을 먼저 await하고, 그 성공 뒤 보존된 route
  intent가 있으면 재연결·검증한다. grant와 start는 assistant-only 경로에서도 진행하며,
  Minecraft allowlist는 route 검증 성공 때만 포함한다. stop 실패는 route·grant·
  start를 실행하지 않고 기존처럼 grant를 폐기해 시작 실패로 닫는다.
- focused Discord command 25개와 자율 승인·router·runtime·Minecraft readiness 인접
  125개, CI-equivalent 전체 3,257개(skip 22)가 통과했다. 실제 Discord·Minecraft·Docker는
  시작하지 않았다.

## Local Voice playback-tail capture continuity

- TTS 재생 중 시작한 발화가 trailing silence 뒤 flush되기 전에 playback owner가
  해제되면 post-TTS suppression에서 영구 폐기되던 경계를 actual segmenter의 30 ms
  block 순서로 재현했다. 첫 threshold 후보와 owner claim/release publication 경합도
  실제 capture thread로 고정했다.
- 첫 후보에서 exact playback generation을 보존한다. 같은 generation이 해제되고 새
  owner·active validation이 없으면 기존 VAD/RMS/필수 화자 검증 뒤 priority queue로
  한 번만 넘기고, STT의 정확한 선두 `이블린`을 확인한 뒤 기존 Local Voice admission을
  사용한다. 현재 owner 취소와 다른 generation 거부, validation 인과성은 유지한다.
- Local Voice 집중 88개, voice 전체 647개(skip 5), atomic race 반복 50회와
  CI-equivalent 전체 3,260개(skip 22), Python 구문·diff check가 통과했다. 실제
  마이크·스피커·Discord·Minecraft·Docker는 시작하지 않았다.

## Main/Fast selected-session continuity ordering

- Main의 대상 session보다 Fast 완료 턴이 새롭지만 다른 Main user/session의 후속
  commit이 owner 전체 `savedAt`을 갱신하면, 대상 Main 문맥이 최신으로 오인되어
  Main/Fast prompt 순서가 뒤집히고 bounded tail에서 새 문맥이 탈락하는 경계를
  실제 checkpoint writer→reader→양방향 bridge 회귀로 재현했다.
- v2 row에 content-free 상대 활동시각 `lastActiveAgoSec`를 기록하고 restart 때
  checkpoint age와 함께 복원한다. verifier는 선택 session 활동시각으로 ordering,
  stale, guild revocation과 reset을 판정한다. revoked target은 revocation 시각을
  경계로 유지하며 선택 대상의 누락·손상 metadata는 cross merge를 거부해 과거를
  최신으로 세탁하지 않는다. public status에는 이 시각·session ID를 추가하지 않았다.
- focused 45개(skip 1), continuity·Discord/Fast/검색/음성 인접 276개(skip 3),
  CI-equivalent 전체 3,262개(skip 22), Python 구문·diff check가 통과했다. 실제
  Discord·Control Page·LLM·마이크·스피커·Minecraft·Docker는 시작하지 않았다.

## Discord voice listener channel-generation isolation

- unknown SSRC의 지연 mapping retry가 listener stop 뒤에도 utterance queue를 다시 채우고,
  내부 `move_to`와 Discord 외부 이동에서 이전 listener의 assembly·STT·reply/TTS가 새
  channel에 이어질 수 있던 경계를 actual client·queue·app-event 회귀로 재현했다. 기존
  TurnScope 취소가 shared ingress worker 자체를 끝내던 경계도 두 guild queue로 재현했다.
- listener generation과 internal client/generation/channel binding을 decode와 Discord-target
  local mic에서 시작해 assembly·dequeue·STT 후·dispatch까지 재검증한다. stop은 tracked
  retry/task와 bounded queue를 정리하고, per-item child 취소로 shared worker를 유지한다.
  channel move는 guild lifecycle lock에서 이전 guild voice TurnScope와 TTS를 정지한 뒤에만
  진행한다. connect·move 동안 pending ingress를 거부한다. warmup은 lock 밖에서 실행해
  새 event의 turn/TTS cleanup을 지연하지 않는다. 내부·외부 channel event는 관측한 exact
  channel에만 cleanup을 재적용해 lock 대기·cleanup 중 stale event의 역이동을 막는다.
  playback이 남거나 `move_to` 반환 뒤 target이 아니면 disconnect와 fixed failure로 닫는다.
- focused 76개, Discord voice 연결·composition·command·playback 인접 211개와 voice 전체
  659개(skip 5), CI-equivalent 전체 3,275개(skip 22), Python 구문·diff check가 통과했다.
  실제 Discord·마이크·스피커·Docker는 시작하지 않았다.

## Turn-trace sink non-interference and type-only fallback

- Voice validation observer가 실패해도 public turn trace를 계속 시도하고, turn-trace
  writer·file·console sink 및 오류 알림 출력의 일반 예외는 원래 turn으로 전파하지 않도록
  격리했다. 취소와 process control signal은 그대로 전파한다.
- 파일·상위 sink fallback은 각각 `[TURN TRACE FILE ERROR] errorType=<exception-type>`,
  `[TURN TRACE SINK ERROR] errorType=<exception-type>`만 남긴다. console fallback JSON도
  `trace_error_type`만 남기며 예외 메시지·경로를 복제하지 않는다. fallback 출력까지
  실패하면 해당 관측 시도만 무음으로 끝낸다. Model-call의 `BaseException`도 trace 전에
  exception type으로 축약한다.
- focused 36개, observability·turn-trace·실제 caller 인접 77개와 CI-equivalent 전체
  3,277개(skip 22), Python 구문·diff check가 통과했다. Discord·마이크·스피커·
  Minecraft·Docker는 시작하지 않았다.

## Discord explicit-memory reply receipt continuity

- 명시적 기억 저장 결과 답변은 memory context receipt가 없는 전용 경로라 기존에는
  response-ready가 `unattributed`로 결박될 수 있었다. actual Discord text handler
  회귀에서 response-ready·history·terminal commit·completion 네 경계를 고정했다.
- memory-write receipt는 저장 mutation evidence로만 보존하고, 사용자에게 전달하는
  결과 답변에는 별도 exact `not_used` response receipt를 네 경계에 동일하게 전달한다.
- focused 20개, Discord text·ingress·receipt/continuity 인접 159개와
  CI-equivalent 전체 3,277개(skip 22)가 통과했다. 검증은 offline이며 실제 Discord나
  사용자 기억에 live write를 수행하지 않았다.

## Discord voice accepted-turn pre-delivery continuity

- reply gate가 수락한 Discord voice 발화도 LLM/TTS 전에 durable user turn으로 남지 않아,
  취소나 hard-exit이 failure finalizer보다 먼저 오면 확정 transcript가 복구되지 않는 경계를
  actual orchestration과 subprocess 종료로 재현했다. 단순 선행 append는 현재 user 질문을
  Main prompt에 두 번 넣고 process-local 표식은 restart retry를 결박하지 못했다.
- `SessionStateStore`가 current turn ID와 정규화된 exact tail을 함께 판정한다. Discord voice는
  user-only mutation과 durable receipt를 room owner·TurnScope·LLM/TTS/playback보다 먼저
  완료한다. 같은 turn/user retry는 history no-op, 같은 문장의 새 turn은 별도 user row이며,
  성공은 exact user-only tail에 assistant/receipt만 붙인다. stale ID·tail·answer·receipt
  mismatch는 fixed error로 닫는다.
- Main prompt는 durable precommit 표식과 exact current turn/user tail이 모두 맞을 때만 저장된
  현재 user row를 projection 복사본에서 한 번 제거한다. local mic은 이 Discord/Main precommit을
  타지 않는다. 실패·취소는 user-only를 유지하고 취소를 재전파하며, durable receipt 직후
  `os._exit`한 fresh process는 exact user-only tail 하나만 복구하고 reply sink를 실행하지 않았다.
- 변경 직결 84개, 인접 156개, voice 전체 667개(skip 5), CI-equivalent 전체
  3,290개(skip 22), Python 구문·diff check가 통과했다. 실제 Discord·마이크·스피커·
  LLM·TTS·Docker는 기동하지 않았다.

## Control Page forced-search exact turn binding

- 실제 `SessionStateStore`·continuity 경로를 오프라인으로 추적했을 때, 기존 turn이 있는
  Control Page session의 다음 강제 검색이 새 turn을 시작하지 않고 이전 ID로 새 문답을
  commit하며 로컬 TTS에도 같은 이전 ID를 전달하는 경계를 확인했다.
- 검색 runtime은 첫 per-session critical section에서 기존 `begin_user_text_turn` 반환 ID와
  새 TurnScope를 결박하고, 느린 검색·합성 await 동안 lock을 놓는다. 최종 critical section은
  exact current scope를 다시 확인한 뒤에만 history·durable commit·TTS를 수행한다. 일반·검색
  TTS task는 완료까지 scope를 넘겨 다음 턴이 stale 재생을 취소하며, done callback은 exact
  old scope만 지운다. 새 상태 owner나 추상화는 추가하지 않았고 불필요해진
  `current_turn_id` wiring은 제거했다.
- 회귀는 실제 `SessionStateStore`와 `TurnScopeRegistry`로 prior 일반 턴을 대기시킨 뒤
  검색을 실행해 일반 턴이 취소되는 방향과, 검색 await 중 후속 일반 턴이 검색 sink를
  0회로 만드는 반대 방향을 모두 검증한다. 일반·검색 TTS task의 successor 취소와 stale
  scope sink 차단도 고정했다. focused 34개, continuity 인접 68개, UI 전체 186개와
  CI-equivalent 전체 3,294개(skip 22),
  Python 구문·diff check가 통과했다. 검증은
  offline이며 실제 Control Page·LLM·TTS·Docker는 시작하거나 교체하지 않았다.

## Discord autonomy-start runtime error producer

- engine 생성·기존 cleanup·새 start 예외가 고정 실패 응답 안에서 소모돼 Discord
  Runtime Errors가 `clear`로 남는 경계를 확인했다. 세 catch를 기존
  `DiscordRuntimeStatus.record_error`에 연결해 `autonomy_start_failed`와 type만 기록한다.
- 관측 기록기 자체가 실패해도 grant 회수와 고정 응답을 끝내며, start 성공 뒤 응답 전송
  실패는 start 실패로 오분류하거나 grant를 회수하지 않는다. cleanup/route/start await 취소는 오류
  기록·응답 없이 grant를 회수한 뒤 재전파한다.
- 관련 82개, Discord I/O 133개, Runtime Errors/Health 인접 70개와 CI-equivalent 전체
  3,298개(skip 22), Python 구문·diff check가 통과했다. 실제 Discord·Minecraft·Docker는
  시작하지 않았다.

## Discord voice post-playback text projection

- Main voice delivery의 final-answer callback이 사용자 text sink 없이 answer 원문을 운영
  stdout에만 남겨, Discord channel에서는 audio를 들어도 같은 답변 text를 보지 못하는
  경계를 확인했다. callback은 content-free `chars=N` 관측만 남기고 실제 projection은
  audio playback과 기존 assistant/history/continuity finalization 경로 반환 뒤로 옮겼다.
- exact playback client/channel을 선행 캡처하고 send 직전에 캡처한 TurnScope가 취소되지
  않았는지와 같은 client/channel identity를 재검증한다. non-validation actual Discord target에 canonical
  visible text를 application-level 한 번만 보내며 local-mic→Discord는 포함하고
  channel 없는 Local Control, validation, pre-send scope cancellation, channel move/client
  replacement는 보내지 않는다. send 자체 취소는 관측·재시도 없이 전파해 전달 여부가
  모호하다. finalizer가 판정한 canonical memory exposure position을 반환하고, non-null이면
  send용 deletion read lease를 required로 다시 획득해 await 전체를 감싼다. stale exposure는
  send 전에 거부한다. 일반 send 실패는
  선행 성공을 뒤집거나 ambiguous retry하지 않는다.
- send 실패는 shared Discord Runtime Errors owner와 voice pipeline observer에 fixed
  `discord_voice_text_delivery_failed`와 exception type만 각각 기록을 시도한다. observer
  자체의 일반 예외는 완료 turn에 전파하지 않고 취소는 그대로 재전파한다. 변경 직결
  55개, voice 전체 668개(skip 5), CI-equivalent 전체 3,299개(skip 22)가 통과했다.
  검증은 offline source/test이며 실제 Discord·마이크·스피커·Docker를 기동하지 않았다.

## Discord voice duplicate cooldown 경계

- 첫 수락 STT가 session에 남은 뒤 동일한 호출을 cooldown 종료 후에도 영구
  `duplicate`로 차단해 LLM·TTS·text sink에 도달하지 않는 경계를 확인했다.
- 새 timestamp나 상태를 추가하지 않고 기존 reply cooldown이 active일 때만 동일·유사
  STT를 duplicate로 거부한다. cooldown 뒤에는 기존 owner·speaker·wake gate를 다시
  적용하므로, 다른 gate가 허용한 exact wake는 duplicate만으로 막히지 않고
  `wake_entry`로 다시 수락될 수 있다. TTS suppression과 recent echo 차단은 유지한다.
- 정책·runtime gate 집중 20개, voice 전체 668개(skip 5), CI-equivalent 전체
  3,299개(skip 22)가 통과했다. 검증은 offline source/test이며 실제 Discord·마이크·
  스피커를 기동하지 않았다.

## Explicit memory principal isolation

- temp vault actual caller 재현에서 한 Discord principal의 direct-confirm note가 다른
  guild/user의 Main과 Fast local prompt에 정상 attributed 기억으로 들어갔다. 기존
  provenance/receipt는 note 근거와 삭제 현재성만 증명했고 노출 권한을 저장·검사하지 않았다.
- `memory.user-confirmation.note.v2`는 trusted Discord guild/person 또는 startup에서 고정한
  Fast configured/local principal을 domain-separated opaque owner scope로 저장한다. 같은 scope를
  action idempotency, Main/Fast recall, retrieval cache v3, SQLite index schema 7, FTS/scan,
  vector/graph 확장, read-only Markdown 재결합과 최종 render에 전달했다. token은 public
  receipt/card/provenance/snapshot/graph에 넣지 않고 scoped core/project는 global hot context에서
  제외했다.
- owner 없는 v1, renamed marker-only, tag/path direct-confirm note는 기존 관리·편집·삭제에는
  남지만 자동 recall에서 제외한다. 기존 guild/session에서 owner를 추정하거나 migration하지
  않으며 새 authenticated `/remember` 근거만 새 scoped note를 만든다.
- caller·cache·legacy·graph·read-only·public projection 집중 333개(skip 1), memory 전체
  307개(skip 1), CI-equivalent 전체 3,306개(skip 22), Python 구문·diff check가 통과했다.
  검증은 offline temp-vault/source tests이며 실제 Discord·Control Page·사용자 memory·Docker를
  기동하거나 수정하지 않았다.

## Discord voice completion commit ordering

- playback 완료 뒤 assistant history를 붙인 다음 선택적 memory write가 동기 `OSError`를 내면
  active follow-up, room owner와 completion continuity commit을 건너뛰어 restart에서 user-only
  precommit만 복구되는 경계를 확인했다.
- 새 owner나 queue를 만들지 않고 기존 memory-exposure guard 안의 순서만 바꿨다. exact
  assistant append→session active/process-local room owner 반영→completion commit 시도를 먼저
  처리하고, benchmark·memory update·cognitive gating·search follow-up은 그 뒤에 둔다. commit이
  durable하면 선택적 예외 전파가 persisted pair와 active follow-up state를 되돌리지 않으며,
  commit 자체의 실패는 기존 고정 오류 경계를 유지한다.
- 실제 `SessionStateStore`·`SessionContinuityCheckpoint`에서 memory update 실패 뒤 fresh restore가
  `[system, user, assistant]`를 보존했다. 변경 직결 17개, voice/continuity 인접 67개, voice 전체
  669개(skip 5), CI-equivalent 전체 3,307개(skip 22), Python 구문·diff check가 통과했다.
  검증은 offline이며 실제 Discord·마이크·스피커·LLM·TTS·Docker는 기동하지 않았다.

## Autonomy delivered-followup terminal boundary

- actual factory engine 두 cycle에서 Discord send 성공 뒤 선택적 memory update `OSError`가
  action을 generic failure/cursor 0으로 바꿔 같은 follow-up과 `[자율봇] 오류`를 각각
  두 번 보내는 경계를 재현했다. `failed`는 blocked retry budget에도 포함되지 않았다.
- send await 정상 반환 직후 900초 ping fence를 세우고, turn/history/active session/
  continuity commit을 선택적 memory/self-state보다 먼저 처리한다. post-send 일반 예외는
  shared Discord Runtime Errors에 `autonomy_followup_finalize_failed`와 type만 기록하되
  verified send와 cursor를 유지한다. observer와 logger 실패도 이를 바꾸지 않고,
  취소·memory deletion integrity 신호는 재전파한다.
- 같은 actual regression은 첫 cycle cursor 1, 둘째 idle, follow-up 1회, 오류 알림 0회,
  continuity durable true와 type-only Runtime Error 1회를 확인한다. 검증은 offline이며
  변경 직결 22개, autonomy/Runtime Errors 인접 160개, CI-equivalent 전체
  3,308개(skip 22), Python 구문·diff check가 통과했다. 실제 Discord·LLM·Docker는
  기동하지 않았다.

## Per-session continuity restore expiry

- `maxAgeSec=60`인 actual store/checkpoint에서 A를 101초 묵힌 뒤 B를 fresh flush했다.
  restart 시 checkpoint age는 1초였고 기존 restore는 effective age 102초인 A와 B를
  모두 복구해, 새 A text turn history에 만료 문맥이 다시 들어갔다.
- row를 store에 투영하기 전에 `checkpoint age + lastActiveAgoSec > maxAgeSec`를
  검사하는 한 경계만 추가했다. 누락·bool·음수·비유한 activity age도 기존 fallback
  값으로 fail-closed한다. raw legacy checkpoint와 generation-0 anchor는 유지하지만
  history는 복구하지 않고, 새 실제 command turn만 v2 generation-1로 chain함을
  기존 migration 회귀로 고정했다. 새 schema·store·owner는 추가하지 않았다.
- continuity 직결 46개(skip 1), 인접 95개(skip 1), CI-equivalent 전체
  3,309개(skip 22), Python 구문·diff check가 통과했다. 검증은 offline이며 실제
  Discord·LLM·Docker를 기동하지 않았다.

## Autonomy stop Runtime Error ownership

- actual Discord composition에서 engine stop `RuntimeError` 뒤 grant 회수와 고정 응답은
  완료되지만 shared Discord Runtime Errors가 빈 상태인 경계와, stop 성공 뒤 성공 응답
  `ConnectionError`가 정지 실패로 오분류돼 두 번째 응답을 시도하는 경계를 재현했다.
- start 명령이 이미 쓰는 composition no-throw observer를 stop에도 전달하고,
  `autonomy_stop_failed`를 runtime allowlist에 추가했다. cleanup try는 `engine.stop()`만
  감싸므로 성공 응답 실패는 관측·재응답 없이 전파되고, stop 취소도 기존처럼 grant가
  회수된 상태로 재전파된다. 새 sink·queue·schema는 추가하지 않았다.
- actual engine child cleanup 대기 중 caller cancel을 기존 suppress가 삼켜 성공 응답까지
  진행하던 경계를 별도 old-red로 고정했다. start와 같은 current-task cancellation 판별을
  재사용해 caller 취소는 `stopping` 상태로 재전파하고, 다음 stop이 disconnect를 다시
  완료해 `idle`로 닫는다.
- 변경 직결 3개, autonomy core 62개, Discord I/O 135개, Runtime Errors 인접 21개,
  CI-equivalent 전체 3,312개(skip 22), Python 구문·diff check가 통과했다. 검증은 offline이며 실제
  Discord·Minecraft·Docker를 기동하지 않았다.

## Person-bound guild raw fallback isolation

- actual temp memory root에서 Discord A의 person-bound raw turn을 저장한 뒤 같은 guild의
  다른 room/user B가 Main memory context를 만들면, 기존 guild raw fallback 때문에 A 원문이
  `attributed`로 prompt에 들어가고 receipt count에도 남는 경계를 재현했다.
- 새 schema나 owner token 없이 기존 scope만 사용했다. person-bound write는 guild raw JSONL
  중복을 생략하고 room/person/사용자 결합 session을 유지한다. person-bound read는 guild raw와
  guild `vault_raw`를 selection·render·count·receipt 전에 빈 layer로 투영한다. 같은 current
  room 공유, person continuity, person key가 없는 local/legacy guild/room 및 요청된 session
  fallback은 유지한다.
- 기존 guild artifact와 일자별 mirror는 disk·legacy coverage에 남고 기존 guild reset과
  일자별 note 관리 동작도 유지한다. owner를 내용·guild/session/evidence에서 추정하거나
  자동 migration하지 않는다. 검증은 offline temp root/source test이며 실제 Discord·사용자
  memory·Docker를 기동하거나 수정하지 않았다.
- actual `record_self_identity_turn`이 수집한 tone/identity review candidate의 owner 없는 queue
  copy가 `render_self_state_context`의 self-model runtime-state에 soft hint로 렌더링되는 sibling
  경계도 확인했다. queue와 사람용 export는 유지하되 renderer가 그 copy를 읽지 않고
  reviewed identity profile만 사용한다.
  같은 턴 원문의 별도 scope-authorized history/raw 사용은 유지하고, 기존 queue를 삭제·owner
  추정·migration하지 않았다.
- privacy 집중 99개, memory 전체 307개(skip 1), CI-equivalent 전체 3,313개(skip 22),
  Python 구문·diff check가 통과했다. 검증은 offline이며 실제 Discord·사용자 memory·배포
  runtime·Docker를 기동하거나 수정하지 않았다.
