# 2026-08-29

## Discord command registry live 체크포인트 최종 회귀

- 전날 승인된 exact guild-only live 검증은 대상 command `0 → 5 → 0`, global `51 → 51`, 다른 guild `0 → 0`,
  보호 run directory 0으로 종료됐다. exact-ID 회수 뒤 독립 read-only 사후 확인도 동일했고 Discord 변경은
  남지 않았다. 서버 이름·guild/application/command ID·credential·command body는 문서에 기록하지 않았다.
- runner 집중 `25 passed, 23 subtests`, command 결합 `145 passed, 42 subtests`, 관련 Python compile,
  PowerShell 5/7 AST와 diff check가 통과했다. 최종 canonical은
  **`5153 passed, 18 skipped, 1 warning, 0 failed`**였다.
- 첫 canonical에서 별개 gateway/Qwen 테스트 2건이 각각 후속 요청 `504`와 `qwen_inference_timeout`으로
  실패했다. 제품 lane과 Qwen admission 시각은 정상이고 최신 Discord 변경과도 무관했으며, 원인은 Windows
  localhost·aiohttp scheduling보다 짧은 50~100ms wall-clock을 의미 검증과 결합한 기존 timing flake였다.
- 제품 timeout이나 구현을 느슨하게 바꾸지 않았다. gateway 테스트는 후속 upstream 진입 event로 lane 재획득을
  직접 확인하고, Qwen 테스트는 controlled future·clock으로 긴 pre-admission queue가 inference budget에서
  제외됨을 검증했다. aiohttp 통합 경로는 별도 scheduler-safe budget으로 유지했다. 관련 파일 전체는
  `36 passed, 14 subtests`, 반복 검증은 gateway `10/10`, Qwen aiohttp `20/20`을 통과했다.
- 잘못 선택한 프로젝트 `.venv`의 전체 실행 1회는 optional Voyager 의존성 3건이 없어 test collection 전에
  중단됐다. 같은 checkpoint를 기존 canonical Python 3.11 환경에서 다시 실행해 위 최종 결과를 얻었으므로
  제품 실패로 집계하지 않는다.
- 이번 후속에서는 Discord, Docker, microphone, Minecraft와 production archive를 시작하거나 변경하지 않았다.
  남은 live 경계는 archive key/TLS/ACL, current-source image provenance, fresh local-mic OFF consent와 실제
  Gateway same-user/wrong-user/stale-session/delivery-failure/ephemeral 180초 interaction이다.
