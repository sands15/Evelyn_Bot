# 2026-08-27

## P0-1 Main LLM finalist Attempt 7 검증 완료

- Attempt 7은 graph-off/on warm `200×2`, restart-ready `30×2`, ABBA macro-block `20`, soak
  `1,000`을 완주하고 `completed`로 종료됐다. 별도 새 Python 프로세스 verifier와 독립 read-only
  감사가 모두 `verified`를 반환했으며 evaluator는 `eligible/candidate_passed/passed`였다.
- graph-off/on warm p50/p95/p99는 `238.7/260.7/290.1 ms` 대 `201.85/219.1/239.8 ms`였다.
  graph-on delta는 p50 `-36.85 ms`, p95 `-41.6 ms`, p99 `-50.3 ms`이고 restart-ready p95는
  `515.3→467.0 ms`였다. paired bootstrap 2,000회의 p95 95% CI는 `[-45.7,-26.7] ms`, effect
  size는 `-3.0166`이었다.
- output equivalence는 `200/200`이고 error/OOM/malformed/stale/duplicate/order/interference,
  quality/safety/privacy/cache proof failure는 모두 0이었다. resident prompt-cache hit ratio는
  양쪽 `0.9052`, GPU 최소 여유는 graph-off/on `9,886/9,768MiB`였다.
- signed cleanup proof는 exact-owned process/GPU allocation/artifact `0/0/0`을 확인했다. signed
  host-restoration proof는 Docker `OFF→OFF`, global container 0, WDDM 안정 관측 3회를 확인했고
  production listener도 0이었다. 결과 보존 후 exact scheduled task
  `EvelynMainLatencyFinalistAttempt7`을 제거하고 부재를 재확인했다.
- canonical artifact는
  `runtime_artifacts/validation/main_latency_finalist_graph0-vs-graph1_sm120_swa1_attempt7.json`,
  SHA-256은 `63a42710fcf4d89663ef5879c32055363c94ec5197f3ffb44dbf6b42f3ad5f2b`다. 이 결과는
  fixed Main→TTS-ready harness의 graph-on 승격 근거이며 microphone, speaker/Discord first-write와
  실제 청취를 포함한 전체 음성 SLO 완료로 확장하지 않는다.

## P0-2 queue·timeout 회귀 수정 완료

- Qwen broker의 request handler가 queue 뒤에도 marker fsync와 session/owner cleanup까지 inference
  deadline에 포함하던 경계를 수정했다. inference clock은 slot·marker claim 뒤 시작하고 검증된 full
  upstream JSON 결과에서 끝나며 queue, delivery ACK, session/owner cleanup과 분리된다.
- Main admission gateway는 각 I/O마다 operation/disconnect/preempt task 세 개를 만들던 fence를 하나의
  cancel-safe polling operation으로 단순화했다. timeout/cancel 때 exact operation을 gather하고, 첫 chunk가
  upstream EOF를 확정하면 불필요한 추가 read를 만들지 않아 다음 REALTIME payload를 보존한다.
- 계획에 고정된 두 exact 회귀는 통과했고 Qwen 회귀는 독립 10회 연속 통과했다. 관련 두 파일 전체는
  `35 passed, 14 subtests passed`를 두 번, reservation/warmup/specialist 인접 묶음은
  `29 passed, 9 subtests passed`를 통과했다. canonical `python -m pytest -q`는
  `4573 passed, 22 skipped, 1391 subtests passed`로 268.30초에 기능 실패 0개였다. 경고는 기존
  Python 3.13 예정 `audioop` deprecation 1개뿐이며 live service는 기동하지 않았다.

## P0-3 capsule 검증과 clean-overlay blocker

- 현재 branch/HEAD/submodule pin은 각각 `codex/omnivoice-tts-cutover`,
  `b5de2920f8287f105a8b1007b5a7daf475d910f8`,
  `b36eaf7e61b3f6bd031fdb531812b2e3c42b6c73`로 설계와 일치했다. 외부 restricted recovery
  capsule은 root untracked 145개, intended stage 후보 109개, raw restricted 28개, scratch 8개,
  tracked dirty 342개와 submodule 복구 자료를 manifest/SHA-256/DACL로 독립 검증했다. manifest
  SHA-256은 `8ccc95350b2c506ddf48e33e320763ac9dcd8c5ffc4be1daead5c59f07e2f9dd`다.
- pinned checkout을 Dockerfile과 같은 16개 line-ending 정규화, 11개 GNU `patch -p1`, parent
  overlay 순서로 재구성하자 `survival_hostile_reflex.test.mjs`의 기존 2개 계약이 결정적으로
  실패했다. Windows timer나 patch 누락이 아니라 `evelyn_survival_mode.js`의
  `shouldDeferHostileExecution` 11줄이 `execute(...)` 전 반환해 ActionManager admission과
  resolved denial 관측을 건너뛰는 source 회귀였다.
- 실제 source는 건드리지 않고 임시 clean tree에서 그 11줄만 제거한 A/B는 exact `2/2`,
  hostile-reflex `25/25`, 나머지 Docker Node test 묶음 `34/34`, `231/231`, `44/44`와 runtime
  verifier `3/3`을 통과했다. 임시 drive mapping은 제거했다. 실제 submodule 정리, hygiene,
  stage, commit, tag, bundle은 실행하지 않았고 staged file은 0개다.
- P0-3은 source 의미 변경을 금지하고 예상 영구 diff를 고정했으므로 이 최소 회귀 수정을 넣는
  것은 승인 범위 변경이다. 또한 계획의 `git apply --check`는 실제 fuzz-tolerant GNU patch
  chain과 불일치해 GNU dry-run gate로 정정해야 한다. 범위 결정 전에는 P0-3과 후속 P0-4/P0-5를
  진행하지 않는다. Docker Desktop/process/pipe와 production listener는 OFF, Attempt 7 task와
  R: mapping은 absent, Wallpaper Engine은 기존 실행 상태임을 재확인했다.

## P0-3 최소 회귀 수정과 clean overlay 재검증

- 사용자가 ActionManager admission을 건너뛰던 exact 11줄 삭제, Docker와 동일한 GNU
  `patch --dry-run -p1` gate, 후속 P0 순서 계속 진행을 승인했다. 실제 source에서는
  `evelyn_survival_mode.js`의 `execute(...)` 전 조기 반환 블록만 삭제했다.
- pinned clean checkout에 16개 line-ending 정규화, GNU patch 11개와 parent overlay를 적용한
  재검증은 patch `11/11`, Node test 파일 `10/10`, runtime verifier `3/3`을 통과했다. receipt는
  pin `b36eaf7e61b3f6bd031fdb531812b2e3c42b6c73`, source submodule untouched를 확인했고 R: mapping은
  제거됐다.
- Microsoft auth cache ignore는 pinned dependency를 직접 수정하지 않고 parent
  `external/mindcraft_evelyn/evelyn.patch`가 build tree의 `_tmp_ms_profiles/`를 제외하도록
  고정했다. hygiene 회귀도 raw pin이 아니라 이 overlay source-of-truth 계약을 검증하며 exact
  focused test는 통과했다.
- capsule manifest에 결박된 submodule overlay/scratch 10개와 backup metadata 63개, 이후 드러난
  generated 2-byte auth-cache placeholder 3개를 exact cleanup하고 tracked 4개를 pin으로 복원했다.
  submodule HEAD와 index pin은 위 commit으로 같고 status는 clean이다. 이 단계에서는 Docker,
  production, microphone, speaker, Discord, Minecraft를 시작하지 않았다.
- 첫 focused run은 overlay에서 관리하는 planner의 generated submodule 복사본을 직접 읽던 stale
  assertion 한 개만 실패했다. parent source 자체의 broker assertions와 Docker copy/clean-overlay
  gate는 유지하고 그 generated-copy 결합 5줄만 제거했다. 재실행한 hygiene, Compose,
  service-manifest, shutdown, Mindcraft contract 묶음은 `122 passed, 29 subtests passed`였다.
- capsule manifest의 exact 450개 경로를 stage하자 누락·추가·금지 경로는 0이었고 raw 28개와
  runtime artifact는 제외됐다. staged `diff --check`가 새 파일에서 추가로 드러낸 ASR 문서 2곳,
  2026-08-17 worklog EOF 1곳과 `combat.patch` 9곳의 non-semantic whitespace를 정규화했다.
- `latency.patch`의 9곳은 GNU unified-diff의 context marker 또는 upstream exact context여서 지우면
  patch hunk가 실패했다. final clean-overlay가 이를 승격 전에 검출했고, 파일을 capsule SHA-256
  `224ffd8c13b9553df677a91cbb928392dffaeac426cdbc39c35407f622b6f90e`와 byte-exact하게 복구했다.
  `.gitattributes`에는 이 exact path만 `text eol=lf -whitespace`로 두어 semantic patch와 default
  `git diff --check`를 함께 보존했다. 수정 뒤 clean overlay는 patch `11/11`, Node test 파일
  `10/10`, verifier `3/3`을 다시 통과했다.
- 첫 local commit `41dc3efa...`와 semantic patch를 고친 `5bfe6a02...`의 tag/bundle은 후속 gate
  실패를 발견한 즉시 accepted 상태로 사용하지 않았고 remote push도 없었다. 기존 bundle은
  rejected evidence로 보존하고 정정 tree로 commit, tag, canonical bundle과 clean clone을 다시
  검증한다.
- pre-correction stage는 manifest와 exact `450/450`, 누락·추가·금지·unstaged·visible untracked
  모두 0이고 canonical `python -m pytest -q`는
  `4573 passed, 22 skipped, 1391 subtests passed`로 254.03초에 기능 실패 0개였다. 경고는 기존
  Python 3.13 예정 `audioop` deprecation 1개뿐이다.
- 정정 bundle clone의 suite는 `4572 passed` 뒤 `bot_profiles/`가 Git의 빈 디렉터리로 보존되지
  않아 combat-matrix preflight fixture 1개만 실패했다. 원본 workspace에는 ignored Microsoft auth
  cache가 디렉터리를 우연히 만들어 이 결합을 가렸다. private cache는 계속 ignore하고
  `bot_profiles/.gitkeep`만 추적해 실제 preflight와 clean clone이 같은 required mount root를 갖게
  했다. 인증 cache 내용이나 이름은 stage하지 않았다.

## P0-3 immutable recovery checkpoint 완료

- 최종 commit은 `d1c8863bc7d338ea6b7ff3013d1e0574dff73e80`, tree는
  `931ccee6ccfac5090b2bb918e15a7869eaf3eecd`다. base 대비 경로는 capsule manifest 450개와
  `.gitattributes`, `bot_profiles/.gitkeep`의 정확한 452개이며 누락·추가·금지·`diff --check`
  오류는 0이다. root와 pinned submodule `b36eaf7e...c73`은 clean이다.
- annotated tag `evelyn-recovery-2026-08-26`은 위 commit으로 peel된다. complete-history Git bundle은
  `git bundle verify`와 `git fsck --strict --no-dangling`을 통과했고 SHA-256은
  `dd8e4bc54397eb289cc20d6c8fdfca2ecd107cab13bed81d812416d47f188dcb`다. sidecar와 restricted
  capsule DACL의 Admin/SYSTEM-only 계약도 독립 감사에서 일치했다.
- bundle의 새 `bundle-clone-attempt3`은 commit/tree/tag/submodule pin과 clean 상태가 원본과
  일치했고 canonical `python -m pytest -q`가
  `4573 passed, 22 skipped, 1391 subtests passed`로 255.13초에 통과했다. 경고는 기존
  `audioop` deprecation 1개뿐이다.
- 종료 관측은 Docker process 0, production listener 0, R: mapping absent, Wallpaper Engine 기존
  실행 상태였다. remote push는 0개이며 rejected 중간 bundle은 증거로 보존했다. P0-3 최종
  verification receipt는 restricted external capsule에만 두었다.

## P0-4 headless STT offline contract 구현

- P0-4 overlap runner를 old/new 각각 warmup 2+measured 20, raw baseline SHA, clean source,
  unique Compose project, exact container/image/model/runtime/GPU와 pre/post stability를 요구하는 v2
  receipt로 확장했다. private ASR runner는 manifest SHA에 결박된 positive 40/negative 10의
  batch+stream aggregate, cancel/successor와 route gate만 기록하고 raw audio/transcript/path는 report에
  남기지 않는다. 기본 cleanup은 evaluation을 durable하게 쓴 뒤 corpus root를 같은 parent의
  content-free quarantine으로 원자 이동하고 exact 51개만 재검증·삭제한다.
- diagnostic Compose는 Main/Qwen의 9820/9823을 loopback에만 열고 Qwen `/llama`와 STT host cache의
  `hub/`만 read-only로 mount한다. STT는 offline/token-disabled 환경과 named log volume을 사용한다.
  STT 전용 allowlist build context, digest-pinned CUDA 12.8 base, two-stage venv, direct dependency pins,
  `pip check`와 sorted package-set SHA를 source에 고정했다. production ASR Python/API/model/memory,
  launcher/admission 동작은 바꾸지 않았다.
- GPU/ASR tool, Compose와 dependency hygiene focused는 `75 passed, 29 subtests passed`, voice 전체는
  `816 passed, 5 skipped, 98 subtests passed`였다. 두 tool의 `py_compile`, merged Compose config와
  `git diff --check`도 통과했다. canonical `python -m pytest -q`는
  `4601 passed, 22 skipped, 1409 subtests passed`로 257.51초에 기능 실패 0개였다. 경고는 기존
  Python 3.13 예정 `audioop` deprecation 1개뿐이다. source test를 live 결과로 승격하지 않는다.
- 고정 private corpus directory와 manifest는 absent이고 audio는 `0/50`이다. 합성 corpus를 정답으로
  만들지 않으므로 full quality/promotion gate는 외부 입력 대기 상태다. 이 checkpoint까지 Docker,
  GPU service, microphone, speaker, Discord와 Minecraft는 시작하지 않았다.
