# 2026-09-03

## Top-level README and Developer Vault refresh

### Verified outcome

- The top-level README had last changed at `d091efe` on 2026-08-11 and omitted 61 later commits through the pre-documentation source baseline `cdf3151`.
- `README.md` now describes Evelyn as the broader local-first assistant runtime, maps the current source layout and turn flow, and separates source/offline, controlled-live, and production evidence.
- The stale memory migration wording was removed. The README now distinguishes the runtime Markdown memory vault, the developer-facing `docs/` vault, and the optional default-OFF private archive.
- Current task/feedback, explicit and automatic memory, revised STT, Discord registry, screen/UI action, Minecraft, startup, privacy, and documentation-routing boundaries are summarized with direct project links.
- The old test example omitted the unittest top-level root and caused `tests/tools` to shadow the repository `tools` package. The documented command now matches CI and includes `-t .`.
- The public Developer Vault reused its existing Evelyn project hub instead of adding duplicate Daily, Learning, Decision, or unfinished W36 review notes. It now links the 2026-09-01~03 milestones and current personal checkpoints.

### Verification

- `python -m unittest discover -s tests -t . -p "test_*.py"`: `5109 tests`, `18 skipped`, failures/errors `0` on the current code baseline.
- The earlier command without `-t .` produced 16 import-collection errors; rerunning the CI command proved they were package-shadowing errors in the old README example rather than product-test failures.
- Python compile, the source-hygiene test, four active JavaScript syntax checks, all 25 local README links, and `git diff --check` passed.
- The Developer Vault self-test and public-content check returned `SELF_TEST_OK` and `PUBLIC_VAULT_CHECK_OK`. Its Markdown-only auto-sync commit `56b9a3c` contains only `Projects/Evelyn.md`, and local `HEAD`, `origin/main`, and the remote `main` ref match.
- No Docker, Discord, model, microphone, speaker, screen-action, or Minecraft service was started. The result is source/offline evidence only.

### Publication boundary

- Before this documentation commit, the Evelyn working branch was 289 commits ahead of the public GitHub main line and did not directly track that remote. Publishing only this documentation would therefore also publish the broader source history, so no GitHub push is inferred from this documentation task.
- `docs/01_NOW.md` is unchanged because project focus, runtime evidence, blockers, and next actions did not change.

### Sources

- `README.md`
- `docs/01_NOW.md`
- `docs/CURRENT_STATE.md`
- `docs/ACTIVE_RISKS.md`
- `docs/DOCUMENTATION_INDEX.md`
