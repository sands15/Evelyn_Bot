# 2026-08-26

## 실행 우선순위 기준점

- 루트 `plan.md`를 앞으로의 단일 실행 계획으로 만들고 문서 인덱스에 연결했다.
- 사용자 결정에 따라 모든 구현을 기본 미승인으로 바꾸고, 8개 설계 완료 조건을 닫은
  뒤 대상별 명시 승인을 받아야만 기계적 구현을 시작하는 절대 게이트를 추가했다.
  구현 중 새 판단이 필요하면 중단·재설계·재승인하며 다음 항목은 자동 시작하지 않는다.
- 최신 source/test/runtime 증거를 대조해 진행 중인 Main finalist 종결, 재현된 admission
  회귀 2개 수정, 455개 worktree 상태 항목의 복구 가능한 기준점, Local/Discord 음성 실환경
  E2E 순으로 우선순위를 고정했다. Minecraft 승인 E2E와 task semantic 기능은 그 뒤다.
- focused 재검증에서 Qwen queue 대기 뒤 inference timeout과 Main upstream header timeout
  뒤 successor 504가 각각 재현됐다. live service는 새로 기동하지 않았다.

## P0-3/P1-1 설계 동결

- P0-3 감사에서 현재 branch/HEAD에 upstream이 없고 dirty local `source` remote, tracked 수정
  342개, untracked 기본 항목 113개, dirty pinned Mindcraft submodule, root 조사 script와 raw
  Live2D 원본이 섞여 있음을 확인했다. restricted external recovery capsule을 hash 검증한 뒤
  parent overlay로 submodule을 복구하고, 단일 checkpoint commit·annotated tag·Git bundle·clean
  clone 전체 suite로 재현성을 증명하는 설계를 동결했다. raw/private 자산과 credential/runtime
  artifact는 Git/docs/bundle에서 제외하고 remote에는 push하지 않는다.
- P1-1 감사에서 Discord 기본 `VOICE_END_SILENCE_SEC=0.82`가 endpoint p95 목표 700ms보다
  길어 현재 설정으로 목표 달성이 불가능함을 확인했다. 기본 0.60초, 현행 owner/streaming/batch
  구조의 content-free timing, `voice-p0.v2`, private 50-item corpus, Local/Discord 각 11-step
  live suite와 exact rollback을 동결했다. packet-time Discord streaming과 새 model/scheduler는
  비범위다.
- 두 항목은 `plan.md`에서 `[R]`이며 구현은 미승인이다. P0-1 terminal restoration과 P0-2
  전체 suite 녹색이 선행되고, Docker·GPU·microphone·speaker·Discord live action은 별도 명시
  승인 뒤에만 수행한다. 이번 checkpoint에서는 code/runtime을 변경하거나 live service를
  시작하지 않았다.

## P0-4 Qwen3.8 모델 교체 설계 동결

- 현행 GPU1 계약을 대조했다. `minecraft_llm`의 Qwen3-14B Q4는 Mindcraft 6 kind,
  specialist와 `/작업` task를 함께 처리하며 context 6144, non-thinking, 6초 inference timeout을
  사용한다. 이전 Qwen+Qwen ASR overlap은 peak 14,039MiB, min free 10,284MiB, Qwen p95
  2,233.2ms, STT p95 626.1ms였다.
- 공식 Qwen3.8에는 14B가 없고 최소 dense model이 27B임을 확인했다. 승격 후보는 pinned
  community standard `Qwen3.8-27B-Q4_K_M.gguf` revision/hash 한 개로 고정했다. Q3 fallback,
  Q5 이상, MTP, mmproj, reasoning, context/timeout 변경은 비범위다.
- 현행 Qwen/model/build는 덮어쓰지 않고 pinned llama.cpp SM86 side-by-side build를
  `minecraft_llm`에만 mount한다. 20-case 역할 A/B, 전체 suite, GPU1 Fast Main+Qwen+STT
  warmup 2+20회, 3회 restart와 exact cleanup이 모두 통과할 때만 승격하며 실패하면 14B로
  rollback하도록 `plan.md` P0-4를 `[R]`로 동결했다.
- 이번 checkpoint에서는 `plan.md`와 project docs만 수정했다. model 다운로드, llama.cpp
  fetch/build, source/config/test 변경, Docker/GPU live action은 실행하지 않았다.

## P0-4 승인 후 선행 gate currentness

- 사용자가 P0-4 전체 구현과 계획에 고정된 제한적 GPU1/Docker live 검증을 승인했다.
- 승인 직후 read-only currentness에서 Main finalist attempt 5의 exact owner processes와
  isolated Docker lab이 계속 실행 중이었고 progress는 warm 200/200, ABBA 20/20,
  restart eligible 40/40, soak 450/1,000이었다. GPU0는 lab이 사용 중이며 GPU1은 0MiB였다.
- P0-2 exact focused tests를 재실행해 Qwen queue wait가 inference timeout을 소비하는 실패와
  Main upstream header timeout 뒤 successor payload가 잘리는 실패를 각각 재현했다.
  `evelyn-recovery-2026-08-26` tag도 아직 없었다.
- 따라서 P0-4를 승인됨·선행 gate 대기로 표시했다. 실행 중 P0-1을 방해하지 않았고,
  model 다운로드, llama.cpp fetch/build, source/config/test 변경 또는 새 Docker/GPU action은
  시작하지 않았다.

## P0-2/P0-3 승인과 P0-1 attempt 5 terminal

- 사용자가 P0-2 회귀 수정과 P0-3 recovery checkpoint 구현을 승인했다. 두 항목은 P0-1
  terminal 완료 전에는 source를 바꾸지 않는 선행 gate에 그대로 결박했다.
- Attempt 5는 warm 200/200, ABBA 20/20, restart eligible 40/40, soak 1,000/1,000을
  끝냈다. exact-owned terminal cleanup은 process/GPU allocation/artifact 0/0/0이고 Docker는
  initial OFF로 복구됐지만 `hostFinalizationError=gpu_restore_timeout`, 최종 status
  `cleanup_required`, scheduled task result 1이었다. GPU1은 0MiB였다.
- Root cause는 baseline 대비 GPU0 free memory 256MiB exact 복귀가 unrelated WDDM allocation
  변화까지 run leak로 취급하는 것과, signed receipt를 host restoration 성공 뒤에만 artifact에
  넣는 ordering이다. Attempt 5 artifact에는 receipt/evaluation이 없어 사후 승격할 수 없다.
- 추가로 finalist driver가 ignored runtime artifact인데 tracked test와 harness identity가 이를
  참조해 P0-3 clean bundle 재현 계약과 충돌함을 확인했다. exact owner cleanup과 Docker gate는
  유지하고 WDDM 3회 util<=10%/free>=75%, host proof 전 receipt atomic persist, tracked driver,
  동일 attempt 6을 사용하는 복구 설계를 `plan.md`에 동결했다.
- 새 사실이 승인된 diff를 바꾸므로 source 수정과 attempt 6 live rerun은 다시 승인받기 전까지
  시작하지 않았다. Attempt 5 artifact/journal은 수정하거나 삭제하지 않았다.
- Attempt 5 최종 progress checkpoint는 graph-off/on warm p50/p95
  `255.9/294.2 ms` 대 `217.1/245.7 ms`였다. graph-on이 `-38.8 ms(-15.16%)`와
  `-48.5 ms(-16.49%)`였지만 이 수치는 signed receipt가 아닌 numeric checkpoint다.
  최종 artifact에 receipt/host-restoration proof/evaluation이 없어 p99, restart-ready p95,
  95% CI, effect size와 오류·품질·cache·GPU gate는 Attempt 5에서 독립 판정할 수 없다.
- 새 Python 프로세스의 `tools/main_latency_finalist_verifier.py`는 terminal artifact를 exit 1로
  거절했다. Docker Desktop/service/engine, production listener, owned lab process/temp는 OFF/0이며
  Wallpaper Engine의 기존 PID는 responsive였다. exact scheduled task
  `EvelynMainLatencyFinalistAttempt5`를 제거하고 전용 heartbeat를 PAUSED로 전환했다. Wrapper가
  `-control play` 결과를 보존하지 않아 Wallpaper의 exact playback 상태는 signed proof가 아니다.

## P0-1 Attempt 6 recovery 구현

- 사용자가 동결한 Attempt 6 복구 source와 제한된 Docker/GPU0 live rerun을 승인했다.
- ignored runtime driver를 내용 보존하여 tracked `tools/main_latency_finalist_driver.py`로 옮기고
  worker harness identity와 test import를 새 path에 결박했다. wrapper만 ignored runtime
  orchestration으로 유지한다.
- signed receipt와 content-free timing diagnostics를 host restoration 전에 atomic state에 보존한다.
  host proof나 evaluation이 실패하면 canonical receipt/evaluation/offline verification 없이 계속
  `cleanup_required`이므로 보존 receipt만으로 승격할 수 없다.
- exact-owned cleanup `0/0/0`, Docker global-empty/original-state gate와 동일 GPU driver/total VRAM,
  연속 3회 utilization `<=10%`는 유지했다. unrelated WDDM 변동은 baseline 대비 256MiB 대신
  physical free ratio `>=75%`로 분리했다.
- host restoration 실패 시 receipt 보존, 75% 이상 WDDM 변동 허용, 75% 미만 거절 회귀를
  추가했다. 9개 latency tool suite는 `165 passed, 166 subtests passed`였다. P0-2의 이미 재현된
  두 실패 때문에 canonical suite는 동결 순서대로 P0-2 이후 실행한다.
- Attempt 5 artifact는 수정·삭제하지 않았고 SHA-256
  `ED345C7036336460417075EFA6916F85AFE048ED10DEE3457595226F6F343CC1`을 유지한다. 동일 workload의
  독립 `runtime_artifacts/validation/run_main_latency_finalist_attempt6.ps1`을 준비했으며 실행 중
  source·Docker·GPU owner를 바꾸지 않는다.

## P0-1 Attempt 6 preflight terminal

- 독립 scheduled task는 Docker initial OFF를 확인하고 tracked driver를 실행했다. host lifecycle,
  startup cleanup `0/0/0`, measurement preflight는 통과했지만 identity discovery가 generic
  `lab_isolation_preflight_failed`를 반환해 observation 0에서 fail-closed했다.
- terminal artifact는 `status=failed`, `errorCode=LabIdentityDiscoveryError`이고 SHA-256은
  `5AFCF236E3916BD0E64D3003C4BA10747595482A359A93DC072586A62794B2FD`다. task result는 1이며
  Docker initial/final OFF, GPU restore `clean/restored`, owned temp 0개를 확인했다.
- discovery가 production/container 부재와 WDDM idle을 한 generic code로 합쳐 exact subcheck를
  보존하지 않음을 확인했다. 종료 후 GPU0 utilization 20%는 transient idle 실패와 일치하지만
  exact 원인 증거로 사용하지 않는다.
- `plan.md`에 GPU-idle 전용 preflight code와 그 code만 15초 간격 최대 3회 재시도하는 Attempt 7
  설계를 동결했다. 새 사실이 승인 diff를 바꾸므로 사용자 재승인 전 source 수정이나 재실행,
  P0-2/P0-3/P0-4를 시작하지 않는다.

## P0-1 Attempt 7 구현 승인과 source 검증

- 사용자는 최초 계획 승인이 같은 범위·안전 경계·완료 gate 안의 실패 진단, 회귀 수정,
  후속 attempt와 검증 반복까지 포함한다고 명확히 했다. 새로운 제품 판단·외부 효과·모델·
  임계값·범위 변경만 재승인 대상으로 유지한다.
- `lab_gpu_idle_preflight_failed`를 기존 fail-closed preflight code 집합에 추가하고 worker의
  production/container 격리 실패와 GPU-idle 실패를 분리했다. WDDM 3회, utilization `<=10%`,
  free `>=75%` 임계값은 바꾸지 않았다.
- driver는 정확히 GPU-idle code일 때만 15초 간격 최대 3회 identity discovery를 재시도한다.
  generic isolation과 identity mismatch는 첫 실패에서 끝나며 artifact에는 attempt count와 마지막
  content-free code만 남는다.
- exact GPU-idle code, transient success, retry exhaustion, generic no-retry 회귀를 추가했다.
  9개 latency tool suite는 `167 passed, 169 subtests passed`였다.
- Attempt 7 wrapper는 Attempt 6과 비교해 artifact/log 이름과 `--attempt 7`만 다르며, 동일 120초
  foreground 이탈과 workload를 유지한다. Qwen/P0-2/P0-3 source와 모델은 변경하지 않았다.

## STT 기준선 우선순위 승인

- 사용자가 Attempt 7 뒤 P0-2/P0-3을 닫고 revised STT image를 현행 Qwen3-14B와 먼저 검증한
  다음 Qwen3.8로 가는 순서를 승인했다.
- headless STT 단계는 P0-4, Qwen3.8 교체는 P0-5로 재번호화했다. P0-4는 STT image만 build/load해
  private 40 positive/10 negative batch/stream corpus, old/new image의 Qwen14 overlap
  warmup 2+measured 20, cancel/successor, 3회 restart와 cleanup을 검증한다.
- microphone, speaker, Discord, Minecraft와 endpoint/playback 실제 음성 E2E는 이 승인 범위가 아니며
  P1-1에 남긴다. Attempt 7 실행 중에는 측정 source·Docker/GPU owner를 변경하지 않는다.

## Main LLM finalist harness retry

- Verified the prior campaign failure was harness/runtime durability, not a completed 200-sample verdict: the finalist reloaded Main for every measured turn and held the only signed result until terminal cleanup.
- Changed the finalist design to 20 paired ABBA macro-blocks with five fixed-history resident observations per leg. Main activation falls from 400 warm activations to 80; Bot-only recreation preserves the original one-prime/one-resident prompt state between observations. The 200 observations per condition remain, while the independent bootstrap unit is explicitly 20 paired macro-blocks.
- Reused evenly spaced first-after-activation observations for the 30 restart-ready cases instead of running 60 additional Main activations.
- Added numeric-only atomic progress checkpoints, preserved signed aggregates across transient cleanup evidence, and kept cleanup as a fail-closed promotion gate.
- Verification: the latency contract/adapter/transport suite passed 94 tests plus 95 subtests. Canonical `python -m pytest -q` reached 4,542 passed, 22 skipped and 1,382 subtests; two failures remain in the pre-existing user-edited Main admission/Qwen timeout tests, outside the changed latency harness files.
- Next: run the graph-off versus graph-on finalist campaign from a Codex-independent scheduled process, then record the signed result and exact cleanup state. Production services remain off.

## Main LLM finalist attempt 4 launch recovery

- The first scheduled launch never entered the Python driver. Windows PowerShell 5.1 promoted the expected stderr from the Docker-off readiness probe to a terminating error under `ErrorActionPreference=Stop`; after that was corrected, the same shell decoded the BOM-less UTF-8 Korean workspace literal as the legacy code page.
- Replaced native readiness calls with an exit-code-preserving quiet helper and removed Korean path literals by resolving the workspace from `PSScriptRoot`. The wrapper now keeps the scheduled task for observable terminal status and records launcher/driver milestones. Windows PowerShell 5.1 parsed the resulting ASCII-only wrapper successfully, and the Docker-off probe regression check reached the following statement with exit code 1 instead of terminating.
- Verified at 2026-08-26 15:22 KST: `EvelynMainLatencyFinalistAttempt4` was Running; attempt 4 and its external runner/owned worker were active; the result artifact was `status=running`; Docker 29.6.1 had only the isolated finalist lab running while production Evelyn containers remained non-running.
- First atomic checkpoint at 15:29 KST: warm baseline/candidate 10/200 each, macro ABBA 1/20, restart-eligible 2/30 each, soak 0/1,000. Partial first-PCM p50/p95 was baseline 231.1/245.9 ms and candidate 198.1/212.3 ms; this is progress evidence, not a final verdict.
- Checkpoint at 16:00 KST: warm baseline/candidate 60/200 each, macro ABBA 6/20, restart-eligible 12/30 each, soak 0/1,000. Partial first-PCM p50/p95 was baseline 240.1/259.9 ms and candidate 207.0/226.6 ms; the task and owned lab remained active and production containers remained non-running.
- Checkpoint at 16:31 KST: warm baseline/candidate 110/200 each, macro ABBA 11/20, restart-eligible 22/30 each, soak 0/1,000. Partial first-PCM p50/p95 was baseline 241.4/278.6 ms and candidate 206.9/241.1 ms; the task and owned lab remained active and production containers remained non-running.
- Checkpoint at 16:56 KST: warm baseline/candidate 150/200 each, macro ABBA 15/20, restart-ready sampling target 30/30 each, soak 0/1,000. Partial first-PCM p50/p95 was baseline 241.4/285.2 ms and candidate 206.8/241.1 ms; this remains an incomplete warm-phase result.
- Checkpoint at 17:31 KST: warm baseline/candidate 200/200 each and macro ABBA 20/20 completed; 40 eligible restart primes per condition cover the fixed 30/30 target. Soak reached 425/1,000. Warm first-PCM p50/p95 was baseline 241.2/279.5 ms and candidate 206.3/239.9 ms; the signed terminal evaluation is still pending.
## Main LLM finalist attempt 4 terminal verdict

- The workload completed: warm 200/200, restart-ready 30/30, soak 1,000, paired ABBA macro-blocks 20/20. The preserved signed receipt passed in-process receipt/cleanup/attestation HMAC and exact-binding compilation before it was written. Its process-local trust root was intentionally not journaled, so the HMAC cannot be independently replayed after process exit; content IDs and bindings were independently recomputed and matched.
- Graph-on versus graph-off warm answer-first-PCM was p50 206.3 versus 241.3 ms (-35.0 ms, -14.5%), p95 239.9 versus 279.5 ms (-39.6 ms, -14.2%), and p99 249.6 versus 297.9 ms (-48.3 ms, -16.2%). Restart-ready p95 was 451.2 versus 482.4 ms (-31.2 ms, -6.5%). The paired-bootstrap p95 delta 95% CI was [-50.4, -27.4] ms with 2,000 replicates and effect size -2.656.
- All error, OOM, malformed/stale/duplicate speech, unsafe-prefix, safety, quality, cache-proof, order, external-interference, focused-test and privacy-test counts were zero. Output equivalence was 200/200; resident prompt-cache hit ratio was 0.9052 for both; minimum free GPU memory was 11,054 MiB baseline and 11,733 MiB candidate, both above the 4,096 MiB gate.
- The official contract verdict is nevertheless `inconclusive / cleanup_required / cleanup`, not eligible. The signed cleanup proof had zero remaining processes and artifacts but one WDDM GPU allocation; the evaluator stops at the cleanup gate before considering the otherwise-passing latency/statistical gates. Artifact SHA-256: `d75eea138c7b3d81d857cc2ab1e24d03a714651bba4ed05418f4d7021ad50875`.
- Root cause: WDDM cleanup required global GPU0 free memory to return within 256 MiB of a 30,538 MiB baseline for three observations while Docker Desktop was still running. The wrapper stopped Docker only after the driver had already signed the dirty proof. That signed receipt cannot be upgraded after the ephemeral key is gone; the cleanup ordering/proof protocol must be fixed and the finalist rerun.
- Final host restoration was verified: Docker Desktop/backend and all owned lab processes were off, production service ports had zero listeners, the exact scheduled task was removed, the sole marker-validated owned temp directory was removed, and no owned temp directories remained. Wallpaper Engine `-control play` returned exit code 0 and its original PID remained responsive. GPU1 was 0 MiB; GPU0 contained only normal desktop/WDDM use, with no Docker/WSL/llama/Python lab process.

## Main LLM finalist cleanup protocol v2

- Split terminal evidence into two signed layers: the runner now proves only exact-owned process/container/temp cleanup, while the fixed coordinator separately proves Docker's original state and three consecutive WDDM baseline-matching observations before the evaluator may issue `eligible`.
- The host guard now waits within a bounded window for three stable pre-run GPU samples instead of failing on one transient utilization sample. It distinguishes a confirmed stopped Docker Desktop from engine/config/permission ambiguity, rejects nonempty Docker after owned startup reconciliation, and re-observes Docker/GPU in the final safety check instead of trusting cached evidence.
- The finalist driver always calls best-effort restoration after a partially failed prepare, never upgrades a dirty signed receipt with later unsigned cleanup, normalizes malformed partial receipts, restores Docker before final evaluation, and downgrades an otherwise eligible result if the last host check is blocked.
- Added a journal-backed authority and `tools/main_latency_finalist_verifier.py`. A completed artifact's receipt, cleanup proof, host-restoration proof, evaluation, content IDs, HMACs, and exact bindings can now be reopened and checked by a fresh Python process. The lifecycle, verifier, and finalist driver sources are included in the pinned harness identity.
- Verified without starting live services: all nine latency tool suites passed with 163 tests and 166 subtests. Focused tests cover transient WDDM settling, Docker unknown-state fail-close, partial-start restoration, pre-run global-empty enforcement, post-proof re-observation, coordinator provider wiring, driver ordering/failure downgrade, dirty-receipt non-upgrade, and fresh-process offline verification.
- Next: launch isolated attempt 5 from `runtime_artifacts/validation/run_main_latency_finalist_attempt5.ps1`, then accept a result only if the fresh-process verifier, final host restoration, quality/error/cache/GPU gates, and statistical contract all pass. Production remains off.
- Attempt 5 launch was verified at 2026-08-26 19:11 KST after replacing a Windows PowerShell 5.1-incompatible multiline `python -c` probe with the fixed `tools.main_latency_host_state_probe` module. The independent task `EvelynMainLatencyFinalistAttempt5` is running with run ID `sha256:c88c0ffbf1c925826b4c7d0cf2a142a248db70d7e3c230265a5b1002489522a5` and candidate ID `sha256:ffa682c90ba9e7893ed7e825c46ff71589c0c8bc90cd39a6857e62facacc5401`.
- Live preflight evidence: Docker's initial state was confirmed stopped; `hostLifecyclePrepared` and `measurementPreflightVerified` are true; only the three isolated run-owned Main gateway/TTS/Main containers were present; no production service ports were listening. This is a launch/preflight checkpoint, not a performance verdict. The active heartbeat watches the Attempt 5 progress/final artifact and must require fresh-process offline verification plus final host restoration before reporting completion.
